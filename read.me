1. Pull all submodules by `git submodule update --init --recursive`

2. run `npm install` or `yarn install`

3. go to `./core` and run `npm install` or `yarn install`

4. run `npm start` on main folder

5. run `npm run build` for base mraid build without google analytics

6. run `npm run build-prod` for production build of all channels.